# ============================================
# Render.com Environment Variables
# Copy and paste these into your Render Web Service Environment Variables
# ============================================

# Application Settings
APP_NAME=Worqo
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:YOUR_APP_KEY_HERE
APP_URL=https://your-app-name.onrender.com

# Database Connection (PostgreSQL)
DB_CONNECTION=pgsql
DB_HOST=dpg-d470v9fdiees73dfp8g0-a
DB_PORT=5432
DB_DATABASE=worqodb
DB_USERNAME=worqodb_user
DB_PASSWORD=WuT9sdEKRxkJhUxGzkebn5IQyhXoYIO6
DB_CHARSET=UTF8
DB_SSLMODE=prefer

# Optional: Database Seeding (set to true only on first deployment)
SEED_DATABASE=false

# Session Configuration
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Cache Configuration
CACHE_DRIVER=file

# Queue Configuration (if using queues)
QUEUE_CONNECTION=sync

# Mail Configuration (if sending emails)
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USERNAME=null
# MAIL_PASSWORD=null
# MAIL_ENCRYPTION=null
# MAIL_FROM_ADDRESS=null
# MAIL_FROM_NAME="${APP_NAME}"

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=error

# ============================================
# IMPORTANT NOTES:
# ============================================
# 1. Replace YOUR_APP_KEY_HERE with your actual APP_KEY
#    Generate it by running: php artisan key:generate --show
#    Or let the deployment generate it automatically (it will create one if not set)
#
# 2. Replace your-app-name.onrender.com with your actual Render app URL
#
# 3. PORT is automatically set by Render - DO NOT set it manually
#
# 4. For first deployment, you may want to set SEED_DATABASE=true
#    to seed initial data, then change it back to false
#
# 5. The database uses internal hostname for best performance
#    This only works when both services are on Render.com
# ============================================

